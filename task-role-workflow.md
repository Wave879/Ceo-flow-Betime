# Task Role Workflow Plan (Admin / Employees + Secretary Bot)

## เป้าหมาย
- เพิ่มระบบสิทธิ์ตาม role ในฐานข้อมูล `employees.role`
- ให้เลขา (LINE Bot) ทำงานเพิ่ม/แก้ไข/ลด(ลบ)งานได้แบบมีขั้นตอนถามข้อมูลชัดเจน
- ข้อมูลที่ยืนยันแล้วต้องไปแสดง/อัปเดตในหน้าเว็บทันที
- ผู้ใช้สามารถปรับสถานะงานและดูอัปเดตล่าสุดได้

## บทบาท (Roles)
- `admin`
  - เพิ่มงาน
  - แก้ไขงาน
  - ลบงาน
  - เปลี่ยนผู้รับผิดชอบ
  - ปิดงาน/เปิดงานใหม่
  - ดูทุกงาน
- `employees`
  - ดูงานที่ตัวเองรับผิดชอบ
  - อัปเดตความคืบหน้า
  - เปลี่ยนสถานะได้เฉพาะงานที่ตัวเองรับผิดชอบ
  - ลบงานไม่ได้

## แหล่งข้อมูล Role
- Collection: `employees`
- Field: `role` (`admin` | `employees`)
- ต้อง map LINE user -> employee ให้ถูกต้องผ่าน `lineUsers`

## คำสั่งหลักในแชตเลขา
- `เพิ่มงาน`
- `แก้ไขงาน`
- `ลบงาน`
- `อัปเดตงาน`
- `ดูสถานะงาน`
- `งานของฉัน`

## ข้อมูลที่เลขาต้องถามก่อนทำงาน
### 1) เพิ่มงาน
- ชื่องาน (จำเป็น)
- ผู้รับผิดชอบ (จำเป็น: 1 คนหรือหลายคน)
- กำหนดส่ง (แนะนำ/ถ้าไม่มีให้ใส่ `-`)
- ประเภทงาน (`team` หรือ `individual`)
- มูลค่างาน (optional)
- รายละเอียดเพิ่มเติม (optional)

ตัวอย่างข้อความตอบกลับเลขา:
- `ต้องการเพิ่มงานค่ะ กรุณาระบุข้อมูลดังนี้`
- `1. ชื่องาน`
- `2. ผู้รับผิดชอบ`
- `3. กำหนดส่ง (YYYY-MM-DD)`
- `4. ประเภทงาน (team/individual)`
- `5. รายละเอียด (ถ้ามี)`

### 2) แก้ไขงาน
- ระบุงาน (ด้วยเลขลำดับ/ชื่อ/Ref)
- ระบุฟิลด์ที่จะแก้ (`ชื่อ`, `ผู้รับผิดชอบ`, `กำหนดส่ง`, `สถานะ`, `รายละเอียด`)
- ค่าที่ต้องการแก้
- ยืนยันก่อนบันทึก

### 3) ลบงาน
- ระบุงาน (Ref หรือชื่อที่ชัดเจน)
- แสดงสรุปงานที่จะลบ
- ขอคำยืนยัน `ยืนยันลบ`

### 4) อัปเดตงาน/สถานะ
- ระบุงาน
- สถานะใหม่ (`pending`, `in-progress`, `completed`)
- note ความคืบหน้าล่าสุด (`lastUpdate`)
- บันทึกเวลา (`lastUpdatedAt`) และผู้แก้ (`lastUpdatedBy`)

## กติกาสิทธิ์ในแชตเลขา
- ถ้า `employees` สั่ง `ลบงาน`:
  - ตอบว่า `สิทธิ์ไม่เพียงพอ คำสั่งนี้สำหรับ admin เท่านั้น`
- ถ้า `employees` แก้งานที่ไม่ใช่งานตัวเอง:
  - ปฏิเสธและแจ้งว่าแก้ได้เฉพาะงานที่รับผิดชอบ
- ถ้า `admin`:
  - ทำได้ทุกคำสั่ง

## โครงสร้างข้อมูลงาน (แนะนำ)
- Collection: `tasks`
- ฟิลด์หลัก:
  - `name`
  - `assignees` (array: id หรือ name ที่ใช้ match ได้จริง)
  - `deadline`
  - `type` (`team` | `individual`)
  - `status` (`pending` | `in-progress` | `completed`)
  - `description`
  - `lastUpdate`
  - `lastUpdatedAt`
  - `lastUpdatedBy`
  - `createdAt`
  - `createdBy`
  - `updatedAt`
  - `updatedBy`

## หน้าเว็บที่ต้องรองรับ
### 1) Task List
- Filter ตาม role:
  - admin เห็นทั้งหมด
  - employees เห็นเฉพาะงานตัวเอง
- แสดง:
  - ชื่องาน
  - สถานะ
  - deadline
  - ผู้รับผิดชอบ
  - last update ล่าสุด

### 2) Task Detail
- แสดง timeline อัปเดตล่าสุด
- ปุ่มเปลี่ยนสถานะ (ตามสิทธิ์)
- ปุ่มแก้ไข/ลบ (เฉพาะ admin)

### 3) Activity ล่าสุด
- section `อัปเดตล่าสุด`
- แสดงว่าใครอัปเดตอะไร เมื่อเวลาใด

## Flow การทำงาน end-to-end
1. ผู้ใช้พิมพ์คำสั่งใน LINE
2. ระบบเช็ก `lineUsers` -> หา employee -> ตรวจ role
3. เลขาถามข้อมูลที่จำเป็นตามคำสั่ง
4. ผู้ใช้ยืนยัน
5. ระบบเขียน Firestore (`tasks`)
6. ตอบกลับผลลัพธ์แบบ Flex message
7. หน้าเว็บอ่านข้อมูลล่าสุดแล้วสะท้อนผลทันที

## Validation ที่ควรมี
- ไม่ให้เพิ่มงานถ้าไม่มี `name` หรือ `assignees`
- `deadline` ต้องเป็นรูปแบบ `YYYY-MM-DD` หรือค่าว่างที่ระบบยอมรับ
- `status` ต้องอยู่ในชุดที่กำหนดเท่านั้น
- การแก้ไข/ลบต้องมี ref ที่ชัดเจน

## ข้อความตอบกลับมาตรฐาน (แนะนำ)
- เพิ่มงานสำเร็จ: `เพิ่มงานเรียบร้อยแล้วค่ะ`
- แก้ไขสำเร็จ: `อัปเดตงานเรียบร้อยแล้วค่ะ`
- ลบสำเร็จ: `ลบงานเรียบร้อยแล้วค่ะ`
- ไม่มีสิทธิ์: `ไม่สามารถทำรายการนี้ได้ เนื่องจากสิทธิ์ไม่เพียงพอ`
- ข้อมูลไม่ครบ: `ข้อมูลยังไม่ครบ กรุณาระบุ...`

## Milestone แนะนำ
1. เพิ่ม role check ใน webhook ทุกคำสั่งเกี่ยวกับงาน
2. ทำ session ถามข้อมูลเพิ่มงาน/แก้ไขงาน/ลบงาน
3. ผูกการตอบกลับ Flex message มาตรฐาน
4. ปรับหน้าเว็บให้ filter ตาม role และโชว์ latest update
5. เพิ่ม audit log การเปลี่ยนแปลงงาน

## Open Questions
- จะใช้ `assignees` เป็น employee id อย่างเดียว หรือเก็บทั้ง id + name?
- คำว่า `employees` จะคงชื่อนี้ หรือเปลี่ยนเป็น `employee` ให้สอดคล้อง?
- ต้องมี soft delete ไหม (ลบแล้วกู้คืนได้)?

## Timeline (Directory Tree + Event Log)
### เป้าหมาย
- ให้หน้า Task Detail แสดง timeline ไล่จากบนลงล่างตามเวลา
- เห็นว่าใครสั่งงาน เมื่อไหร่, ใครอัปเดตสถานะ เมื่อไหร่, และงานสิ้นสุดเมื่อใด

### Directory Tree (แนะนำ)
```text
tasks/{taskId}
├─ name
├─ status
├─ createdAt
├─ createdBy
├─ updatedAt
├─ updatedBy
└─ timeline/{eventId}
   ├─ type                # task_created | status_updated | progress_updated | reassigned | task_completed | task_deleted
   ├─ title               # ข้อความสั้นสำหรับโชว์หน้าเว็บ
   ├─ detail              # รายละเอียดเพิ่มเติม
   ├─ fromStatus          # optional
   ├─ toStatus            # optional
   ├─ actorId             # employee id หรือ line user id
   ├─ actorName           # ชื่อคนที่ทำรายการ
   ├─ actorRole           # admin | employees
   ├─ source              # web | line_bot | system
   ├─ createdAt           # เวลาเกิดเหตุการณ์ (ISO)
   └─ sortKey             # ใช้เรียงเวลา เช่น timestamp
```

### Event ที่ต้องเก็บขั้นต่ำ
1. `task_created`
- ตอนสั่งงานครั้งแรก (ใครสั่ง, สั่งเมื่อไหร่)
2. `status_updated`
- ทุกครั้งที่เปลี่ยนสถานะ เช่น `pending -> in-progress`
3. `progress_updated`
- อัปเดตความคืบหน้า/หมายเหตุล่าสุด
4. `task_completed`
- ตอนส่งงานเสร็จ ให้ขึ้นสถานะสิ้นสุดชัดเจน

### กติกาแสดงผลบนหน้าเว็บ
1. เรียงจากใหม่ไปเก่า (latest first) หรือเก่าไปใหม่ (timeline mode) ให้เลือกแบบเดียวทั้งระบบ
2. ทุกรายการต้องโชว์ 4 อย่าง:
- เวลา
- ผู้กระทำ (ชื่อ + role)
- ประเภทเหตุการณ์
- เนื้อหาที่เปลี่ยน
3. เมื่อ `type = task_completed` ให้แสดง badge `สิ้นสุดงาน` สีเขียว
4. ถ้างานถูกเปิดใหม่จาก completed ให้เพิ่ม event `status_updated` (completed -> in-progress)

### ตัวอย่างข้อความบน Timeline
- `08 มี.ค. 2026 09:20 - เวฟ (admin) สั่งงาน: TOR ตรวจสอบ`
- `08 มี.ค. 2026 11:05 - พี่ป๊อป (employees) อัปเดตสถานะ: pending -> in-progress`
- `09 มี.ค. 2026 16:40 - พี่ป๊อป (employees) อัปเดตความคืบหน้า: ส่งไฟล์แนบรอบแรก`
- `10 มี.ค. 2026 18:10 - พี่ป๊อป (employees) ส่งงานแล้ว (สิ้นสุด)`

### Validation สำหรับ Timeline
- ห้ามเขียน event ถ้าไม่มี `actorId` และ `createdAt`
- `status_updated` ต้องมี `fromStatus` และ `toStatus`
- ถ้า `toStatus = completed` ต้องสร้าง `task_completed` event เพิ่มเสมอ

## Secretary Persona (Azure OpenAI) - ให้เลขาดูมีชีวิตจริง
### แนวคิด
- ทุกคำตอบของเลขาต้องมีบุคลิกเดียวกัน: สุภาพ ฉลาด กระชับ แต่มี warmth
- ใช้ Azure OpenAI เป็นตัวควบคุมโทนภาษาในทุกจุด: ทักทาย, ถามข้อมูล, ยืนยัน, แจ้งผล, แจ้งเตือนผิดพลาด
- เป้าหมายคือให้ผู้ใช้รู้สึกคุยกับเลขาคนเดิมตลอด ไม่ใช่ระบบตอบแข็งๆ

### Character Profile (คงที่ทั้งระบบ)
- ชื่อผู้ช่วย: `เลขา TeamFlow`
- สไตล์: สุภาพมืออาชีพ, proactive, จัดลำดับเก่ง
- โทนภาษา: ไทยเป็นหลัก, ใช้คำลงท้าย `ค่ะ`
- หลักการสื่อสาร:
  1. อธิบายชัดว่า "ต้องการข้อมูลอะไร"
  2. บอกเหตุผลสั้นๆ ว่าทำไมต้องใช้ข้อมูลนั้น
  3. มีสรุปก่อนยืนยันทุกครั้ง
  4. เมื่อสำเร็จต้องบอกผล + next step

### ใช้ Azure OpenAI ในจุดไหนบ้าง
1. `Intent Clarification`
- ถ้าข้อความไม่ชัด ให้เลขาถามกลับแบบเป็นธรรมชาติ
2. `Slot Filling Dialogue`
- ตอนเพิ่ม/แก้/ลบงาน เลขาถามข้อมูลทีละช่องแบบ conversational
3. `Confirmation Message`
- สรุปข้อมูลก่อนบันทึกในภาษาคน
4. `Status Narrative`
- แปลงข้อมูลสถานะเป็นข้อความอ่านง่าย + ให้กำลังใจทีมแบบพอดี
5. `Timeline Narrative`
- แปลง log เป็นไทม์ไลน์ที่อ่านแล้วเข้าใจทันที
6. `Error Recovery`
- ถ้าข้อมูลไม่ครบ/ไม่ถูกต้อง ให้แนะนำทางแก้แบบสุภาพ

### ตัวอย่าง Prompt Policy (System)
```text
You are "เลขา TeamFlow", a professional Thai executive secretary.
Tone: polite, warm, concise, action-oriented.
Always:
- Ask only necessary missing fields.
- Explain what field is required and why.
- Summarize collected info before final confirmation.
- Use Thai language with "ค่ะ" ending.
- Keep responses short, clear, and human.
```

### ตัวอย่างรูปแบบคำตอบ (Template)
1. เริ่มคำสั่งเพิ่มงาน
- `รับทราบค่ะ เดี๋ยวเลขาช่วยเพิ่มงานให้นะคะ`
- `ขอข้อมูล 3 อย่างก่อนค่ะ: ชื่องาน, ผู้รับผิดชอบ, กำหนดส่ง`

2. ข้อมูลไม่ครบ
- `ตอนนี้ยังขาด "กำหนดส่ง" ค่ะ เพื่อให้ระบบเตือนได้ตรงเวลา`
- `รบกวนส่งเป็นรูปแบบ YYYY-MM-DD นะคะ`

3. ก่อนบันทึก
- `เลขาสรุปก่อนบันทึกนะคะ`
- `งาน: ... | ผู้รับผิดชอบ: ... | กำหนดส่ง: ...`
- `ถ้าถูกต้องพิมพ์ "ยืนยัน" ได้เลยค่ะ`

4. บันทึกสำเร็จ
- `บันทึกเรียบร้อยแล้วค่ะ ✅`
- `เลขาได้ส่งงานขึ้นหน้าเว็บแล้ว และจะติดตามสถานะให้ต่อค่ะ`

5. ปฏิเสธตามสิทธิ์
- `รายการนี้ต้องใช้สิทธิ์ admin ค่ะ`
- `ถ้าต้องการ เลขาส่งคำขอให้หัวหน้าช่วยอนุมัติแทนได้ค่ะ`

### Dynamic Personalization
- ดึงชื่อเล่นผู้ใช้จาก `employees.name`
- เรียกชื่อผู้ใช้ในประโยคแรกเพื่อเพิ่มความเป็นมนุษย์
- ยกตัวอย่าง:
  - `พี่ป๊อปคะ เลขาพร้อมช่วยอัปเดตงานแล้วค่ะ`

### Response Style Rules
- ความยาว: ไม่เกิน 4-6 บรรทัดต่อข้อความทั่วไป
- หลีกเลี่ยงศัพท์เทคนิคเกินจำเป็น
- ถ้าข้อมูลยาว ให้ส่งเป็น 2 ชุด: สรุปสั้น + ปุ่มดูรายละเอียด
- ทุกข้อความสำคัญมี CTA ชัดเจน เช่น `พิมพ์ "ยืนยัน"`, `พิมพ์ "สถานะ"`

### Azure OpenAI Config (แนะนำ)
- Env ที่ใช้:
  - `AZURE_OPENAI_ENDPOINT`
  - `AZURE_OPENAI_API_KEY`
  - `AZURE_OPENAI_DEPLOYMENT`
  - `AZURE_OPENAI_API_VERSION`
- แนะนำแยก deployment:
  1. `secretary-dialogue` (chat โต้ตอบทั่วไป)
  2. `secretary-summary` (สรุป/เรียบเรียง timeline)

### Guardrails
- ห้ามเลขาเดาข้อมูลสำคัญที่ผู้ใช้ไม่ได้ยืนยัน
- ห้ามเปลี่ยนสถานะ/ลบงานโดยไม่มีคำยืนยัน
- ถ้าข้อมูลไม่แน่ใจ ให้ถามซ้ำแบบสุภาพ
- เก็บ audit trail ทุก action ที่เลขาดำเนินการ

### KPI วัดความ "มีชีวิต"
- ผู้ใช้ตอบกลับคำถามของเลขาภายใน 1-2 รอบ (ไม่สับสน)
- อัตราคำสั่งสำเร็จต่อ session สูงขึ้น
- ข้อความผิดพลาด/ถามซ้ำลดลง
- ผู้ใช้ให้ feedback ว่า "คุยเหมือนเลขาจริง"
